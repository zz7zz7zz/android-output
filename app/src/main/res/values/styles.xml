<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="list_scrollbar_style">
        <item name="android:cacheColorHint">@color/transparent</item>
        <item name="android:listSelector">@color/transparent</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:scrollbarThumbVertical">@color/scrollbar_thumb_vertical_color</item>
        <item name="android:scrollbarTrackVertical">@color/transparent</item>
        <item name="android:scrollbarSize">3.33dp</item>
        <item name="android:scrollbars">vertical</item>
    </style>
</resources>
